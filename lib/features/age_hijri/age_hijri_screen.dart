import 'package:flutter/material.dart';

class AgeHijriScreen extends StatelessWidget {
  const AgeHijriScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Hijriah dan Umur')),
      body: const Center(child: Text('Modul konversi Hijriah dan umur siap dikembangkan.')),
    );
  }
}
