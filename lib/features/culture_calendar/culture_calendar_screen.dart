import 'package:flutter/material.dart';

class CultureCalendarScreen extends StatelessWidget {
  const CultureCalendarScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Kalender Budaya')),
      body: const Center(child: Text('Modul Weton Jawa dan Saka Bali siap dikembangkan.')),
    );
  }
}
