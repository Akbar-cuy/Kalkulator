import 'package:flutter/material.dart';

class WorkoutCrudScreen extends StatelessWidget {
  const WorkoutCrudScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Jadwal Workout')),
      body: const Center(child: Text('Modul CRUD workout siap dikembangkan.')),
    );
  }
}
