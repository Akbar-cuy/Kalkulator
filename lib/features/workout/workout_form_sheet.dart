import 'package:flutter/material.dart';

class WorkoutFormSheet extends StatelessWidget {
  const WorkoutFormSheet({super.key});

  @override
  Widget build(BuildContext context) {
    return const Padding(
      padding: EdgeInsets.all(24),
      child: Text('Form workout create/edit'),
    );
  }
}
